# Arrays - Day 02

Today I started learning Arrays from first principles instead of jumping directly into problem solving.

## What I Learned

- What an array is
- Why arrays were invented
- How arrays are stored in contiguous memory
- Why indexing starts from 0
- Basic array operations
- Time and Space Complexity
- Advantages and limitations of arrays

## Problem Solved

### Largest Element in an Array

### Approach

- Assume the first element is the largest.
- Traverse the array once.
- Compare each element with the current largest.
- Update the largest whenever a bigger element is found.
- Return the final largest element.

**Time Complexity:** `O(n)`

**Space Complexity:** `O(1)`

## Additional Learning

I also learned different ways to take array input in Python.

- Hardcoded input
- User input
- Competitive programming input using `list(map(int, input().split()))`
- How `input()`, `split()`, `map()` and `list()` work internally

## Key Takeaway

Today's biggest learning was understanding **why** the algorithm works instead of memorizing the code.

---

**Next:** Second Largest Element in an Array